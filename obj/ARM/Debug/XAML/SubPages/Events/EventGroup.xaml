<Page x:ConnectionId='1'
    x:Class="Salary_Control.XAML.SubPages.EventGroup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Salary_Control.XAML.SubPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mctoolkit="using:Microsoft.Toolkit.Uwp.UI.Converters" xmlns:entities="using:Salary_Control.Source.API.Entities"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <mctoolkit:BoolNegationConverter x:Key="boolToNegative"/>
    </Page.Resources>
    
    <Grid>

        <StackPanel Orientation="Vertical">
            <CommandBar DefaultLabelPosition="Right">
                <AppBarButton x:ConnectionId='17' 
					Icon="ClearSelection" 
					Label="Очистить выделение"
					                      
				    />
                <AppBarButton x:ConnectionId='18' 
					Icon="Delete" 
					Label="Удалить выбранные события"
					                                             
					/>
                <AppBarButton x:ConnectionId='19' 
					Icon="Delete" 
					Label="Удалить всё"
					                                        
					/>
            </CommandBar>
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Vertical" Margin="10 10 0 0">
                    <CalendarDatePicker x:ConnectionId='9'
                                                                   
                        Header="Выберите дату группы событий"
                        />

                    <RichTextBlock>
                        <Paragraph>
                            <Run>Cобытия, которые произошли в этот день:</Run>
                            <Run x:ConnectionId='16'                                                            />
                        </Paragraph>
                    </RichTextBlock>

                    <ScrollViewer>
                        <ListView x:ConnectionId='11' 
                            x:Name="list" 
                                                                     
                                                                                                         
                            >
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            
                            <ListView.ItemTemplate>
                                <DataTemplate                            >
                                    <Grid x:ConnectionId='13' Background="Red">
                                        <Grid.ContextFlyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem x:ConnectionId='14' 
                                                    Text="Изменить событие" 
                                                                          
                                                                                   
                                                    >
                                                    <MenuFlyoutItem.Icon>
                                                        <SymbolIcon Symbol="Edit"/>
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutItem x:ConnectionId='15' 
                                                    Text="Удалить событие" 
                                                                       
                                                                                   
                                                    >
                                                    <MenuFlyoutItem.Icon>
                                                        <SymbolIcon Symbol="Delete"/>
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                            </MenuFlyout>
                                        </Grid.ContextFlyout>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </ScrollViewer>

                    <StackPanel x:ConnectionId='10' 
                        Orientation="Vertical" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                                                                                                  
                        >
                        <TextBlock Text="Похоже, что в этой группе ещё нет событий"/>
                    </StackPanel>
                </StackPanel>


                <Grid x:ConnectionId='2'                                                                   >
                    <TextBlock>Создать событие</TextBlock>
                    <StackPanel Orientation="Vertical">
                        <TextBox x:ConnectionId='5' x:Name="newEventName"/>
                        <TextBox x:ConnectionId='6' x:Name="newEventCost" Margin="0 10 0 10" InputScope="Number"/>
                        <AutoSuggestBox x:ConnectionId='7' x:Name="newEventCategory"/>

                        <Button x:ConnectionId='8'                     Margin="0 10 0 0">
                            Создать событие
                        </Button>
                    </StackPanel>
                </Grid>



                <Grid x:ConnectionId='3'                                                                    >
                    <StackPanel Orientation="Vertical">
                        <TextBlock>wefewef</TextBlock>
                    </StackPanel>
                </Grid>

                <Button x:ConnectionId='4'                       >
                    change
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>

