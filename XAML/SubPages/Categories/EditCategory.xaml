<Page
    x:Class="Salary_Control.XAML.SubPages.EditCategory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Salary_Control.XAML.SubPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Button VerticalAlignment="Top" Margin="10" Click="RedirectBack">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon 
                    Symbol="Back"
                    VerticalAlignment="Center"
                    Margin="0 0 5 0"
                    />
                <TextBlock>Назад</TextBlock>
            </StackPanel>
        </Button>

        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Vertical">
                <TextBox 
                    PlaceholderText="Название категории" 
                    Margin="0 0 0 20"
                    x:Name="categoryName"
                    Text="{x:Bind Params.Category.Name}"
                    />
                <ColorPicker
                    IsColorChannelTextInputVisible="False"
                    IsHexInputVisible="False"
                    x:Name="categoryColor"
                    Color="{x:Bind HexToColor(Params.Category.Color)}"
                    />

                <Button Margin="0 20 0 0" HorizontalAlignment="Center" Click="SaveCategory">
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon 
                    Symbol="Save"
                    VerticalAlignment="Center"
                    Margin="0 0 5 0"
                    />
                        <TextBlock>Сохранить</TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
